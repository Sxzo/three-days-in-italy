# Copy to .env and fill in. Only pipeline/build_places.py reads this; the
# server loads the prebuilt data/places.build.json and never calls the API.
# A rebuild that introduces no new hour strings is served entirely from
# data/hours_llm_cache.json, so this key is only needed when that cache misses.
OPENAI_API_KEY=sk-...
